# Basic docker compose with configuration server

Restore cache from remote http.

run:

```
docker compose up
```

then go to: http://localhost/basic/france_parts?SERVICE=WFS&REQUEST=GetCapabilities
